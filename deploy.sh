#!/usr/bin/env bash
# Datei obsolet. Deployment erfolgt jetzt ausschließlich über:
#   git subtree push --prefix mc_test_app <remote> main
# Diese Datei kann gelöscht werden:
#   git rm mc_test_app/deploy.sh && git commit -m "remove obsolete deploy script"
