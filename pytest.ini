[pytest]
# Only collect files that start with `test_` (prefix), avoid collecting `*_test.py` suffix patterns
python_files = test_*.py

# Prevent recursing into helper/script folders that contain files with colliding module names
norecursedirs = .venv scripts tools out var __pycache__

# Keep output concise
addopts = -q
