# Beispiel-Umgebungsvariablen
# Kopiere diese Datei nach `.env` und passe Werte an.
# Die App lädt `.env` automatisch beim Start (python-dotenv).

# App-URL für QR-Code im PDF-Export
# Für lokale Tests: http://localhost:8501
# Für Online-Deployment: Die öffentliche URL deiner App (z.B. https://your-app.streamlit.app)
APP_URL="http://localhost:8501"

# Admin-Schlüssel für erweiterte Ansicht (Leaderboard + Rohdaten + Reset).
# WICHTIG FÜR KURSTEILNEHMER/INNEN:
#   - Auf "" (leerer String) lassen für direkten Zugang zum Admin-Panel
#   - Das Admin-Panel ist dann nur für Nutzer/innen mit dem Admin-Pseudonym zugänglich
#   - Kein Passwort erforderlich beim lokalen Testen!
# Für Produktiv-Deployment: Setze hier einen sicheren Key (z.B. "mein_geheimes_passwort")
MC_TEST_ADMIN_KEY=""

# Optional: Beschränkt Admin-Rechte auf genau dieses Pseudonym.
# Wenn leer, kann jeder Benutzer (mit korrekt gesetztem Key oder beliebiger Eingabe ohne Key)
# Admin werden.
# Für Kursteilnehmer/innen: "Albert Einstein" als Admin-Pseudonym verwenden.
# SO FUNKTIONIERT ES:
#   1. Starte die App mit: streamlit run app.py
#   2. Wähle auf der Startseite das Fragenset aus
#   3. Wähle als Pseudonym "Albert Einstein" aus der Liste
#   4. Klicke auf "Test starten"
#   5. In der Sidebar erscheint nun der Menüpunkt "Admin-Panel"
#   6. Dort kannst du:
#      - Alle Test-Sessions ansehen
#      - Statistiken und Analytics einsehen
#      - Itemanalyse durchführen (Schwierigkeitsgrad, Trennschärfe)
#      - Distraktor-Analyse (welche falschen Antworten werden oft gewählt)
#      - Feedback von Teilnehmer/innen einsehen
#      - Datenbank zurücksetzen (für neue Tests)
MC_TEST_ADMIN_USER="Albert Einstein"

# Optional: Mindestsekunden zwischen zwei Antworten (Throttling / Rate Limit)
MC_TEST_MIN_SECONDS_BETWEEN=3

# Shared cooldown (seconds) for PDF exports (Testbericht, Musterlösung, Mini-Glossar).
# Default: 300 seconds (5 minutes). Adjust this to be more or less restrictive.
EXPORT_COOLDOWN_SECONDS=300

# (Optional) Weitere Variablen könnten hier hinzugefügt werden.

################################################################################
# Environment template: Local development vs. Cloud deployment recommendations
################################################################################

# LOCAL DEVELOPMENT (use these when running on your laptop)
# - Faster cache limits are acceptable locally.
# - You can copy this file to `.env` and tweak values.

# App URL for local testing
APP_URL="http://localhost:8501"

# Formula cache (local) - use a larger cache for faster local iteration
FORMULA_CACHE_DIR="./var/formula_cache"
FORMULA_CACHE_MAX_FILES=200
FORMULA_CACHE_MAX_MB=200
FORMULA_CACHE_TTL_DAYS=14

# Concurrency for formula rendering (local)
FORMULA_RENDER_PARALLEL=8
FORMULA_RENDER_TOTAL_TIMEOUT=30


# -----------------------------------------------------------------------------
# CLOUD DEPLOYMENT (recommended conservative defaults for ephemeral clouds)
# - Use these conservative defaults for Streamlit Community Cloud or similar
# - Limits keep disk usage bounded on ephemeral file systems
# -----------------------------------------------------------------------------

# Formula cache (cloud)
# Path where rendered formula PNGs are cached (app will try to create it)
FORMULA_CACHE_DIR="./var/formula_cache"
# Conservative limits for cloud deploys
FORMULA_CACHE_MAX_FILES=100
FORMULA_CACHE_MAX_MB=50
FORMULA_CACHE_TTL_DAYS=7

# Concurrency tuned for cloud workers
FORMULA_RENDER_PARALLEL=6
FORMULA_RENDER_TOTAL_TIMEOUT=15
